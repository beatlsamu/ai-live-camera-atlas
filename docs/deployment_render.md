# Render Deployment

1. Create two web services from `render.yaml`
2. Set the backend service environment variables
3. Set the frontend `VITE_API_BASE_URL`
4. Deploy the backend first, then the frontend
